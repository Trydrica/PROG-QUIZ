services:
  - type: web
    name: prog-quiz
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 1 --threads 2 --keep-alive 120
    healthCheckPath: /
    autoDeploy: true